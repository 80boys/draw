export type drawData = {
    x:number,
    y:number,
    n:number,
    c:string
}
export type formPoint = {
    x:number,
    y:number
}
export type win = { 
    width: number,
    height: number 
}

export type updateColor = (arg0: string) => void